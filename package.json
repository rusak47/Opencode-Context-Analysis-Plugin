{
  "name": "opencode-context-analysis",
  "version": "1.0.0",
  "description": "OpenCode plugin for detailed token usage analysis",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "peerDependencies": {
    "@opencode-ai/plugin": ">=1.0.0"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "^1.4.3",
    "@types/node": "^26.1.1",
    "typescript": "^5.3.0"
  }
}
